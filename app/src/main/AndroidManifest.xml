<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nacho.trabajo_obligatorio_11_12_2017">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Model.MyAplication"
        android:allowBackup="true"
        android:icon="@drawable/restaurant"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=""> </activity> -->
        <activity android:name=".View.Search" />
        <activity android:name=".View.Login" />
        <activity android:name=".View.Registro" />
        <activity android:name=".View.Mostrar_Detalle_Imagen" />
        <activity android:name=".View.Carrito" />
        <activity android:name=".View.ResultadoSearch"></activity>
    </application>

</manifest>